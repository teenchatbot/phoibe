# check to see if there are new or changed files and if there are, move them



## check the hashes of each file against the one in the updated repo
### pros: this could work to tell if a file is changed and not just created
### cons: might take up large amounts of memory


## check if a file in the updated version does not exist and if not, move it
